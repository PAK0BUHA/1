#include "pch.h"
#include <iostream>
#define _USE_MATH_DEFINES
#include "math.h"

using namespace std;

int main()
{
	setlocale(LC_ALL, "rus");
	double x, y, z, a, b;
	cout << "Введите значение X: ";
	cin >> x;
	cout << endl << "Введите значение y: ";
	cin >> y;
	b = y;
	if (b < x)
	{
		b = x;
	}
	a = M_PI;
	if (y - x > a)
	{
		a = y - x;
	}
	if (a > b)
	{
		a = b;
	}
	z = a / b;
	cout << endl << "Ответ: z = " << z;
}
